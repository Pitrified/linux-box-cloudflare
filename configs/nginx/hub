server {
    # loopback only: traffic must arrive via the cloudflared tunnel, never the LAN
    listen 127.0.0.1:8090;
    server_name localhost;

    root /var/www/hub;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
