# dynamic graph

## overview

let's simplify the script to just generate the json, use that from js side to build the graph (eg with d3js force directed linkage).

how complex would that be?

update only library page for now,
move dynamic graph to the top,
leave the independent bubbles with details below

## ...
